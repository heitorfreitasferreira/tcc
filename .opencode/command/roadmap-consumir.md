---
description: [DEPRECIADO] Use /proximo — executa a próxima tarefa pendente da fila ordenada
---

# /roadmap-consumir

> **Este comando foi substituído por `/proximo`**, que consome a fila ordenada de tarefas em `vault/roadmap/tarefas/`.
>
> Use `/proximo` para o mesmo efeito, com ordenação por fase e integração com o sistema de log.
