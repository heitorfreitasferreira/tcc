# P37 Judge 2 Recheck

## Verdict

PASS

## Findings

- Nenhum bloqueio encontrado nos cinco capítulos modificados para o escopo de P37.
- Os dois títulos que anteriormente continham `Lower Bound` manual foram corrigidos para usar `\texorpdfstring{\ac{LB}}{Lower Bound}`.
- As ocorrências remanescentes de `Lower Bound`, `TSP` e `AP` em títulos revisados aparecem apenas como texto alternativo de PDF dentro de `\texorpdfstring`, conforme permitido pela solicitação.
- Não foram encontrados usos de `\ac{...}` ou `\acp{...}` dentro de matemática nos cinco capítulos modificados.
- Todas as chaves usadas por `\ac{...}`/`\acp{...}` nos capítulos modificados estão definidas em `monografia/abrev/Abreviaturas.tex`.

## Evidence

- Roadmap P37: `vault/writing/planejamento/roadmap-monografia.md:477` pede substituir siglas manuais por `\ac{}` em todos os 5 capítulos, usando as definições de `abrev/Abreviaturas.tex`.
- Arquivos de capítulo modificados verificados: `monografia/cap_introducao/introducao.tex`, `monografia/cap_fundamentacao/fundamentacao.tex`, `monografia/cap_proposta/proposta.tex`, `monografia/cap_experimentos/experimentos.tex`, `monografia/cap_conclusao/conclusao.tex`.
- Arquivo de siglas verificado: `monografia/abrev/Abreviaturas.tex` define `ACO`, `AP`, `BF`, `CD`, `GA`, `LB`, `NP`, `OX`, `PSO`, `TSP`, `TSP-SD-ATP`, `VANT`, entre outras.
- Título corrigido em `monografia/cap_proposta/proposta.tex:63`: `\section{\texorpdfstring{\ac{LB}}{Lower Bound} por Relaxação \texorpdfstring{\ac{AP}}{AP}}`.
- Título corrigido em `monografia/cap_experimentos/experimentos.tex:92`: `\subsection{\texorpdfstring{\ac{LB}}{Lower Bound} \texorpdfstring{\ac{AP}}{AP}}`.
- Outro uso aceitável de texto alternativo PDF: `monografia/cap_fundamentacao/fundamentacao.tex:45` usa `\section{Roteamento de Drones e Variantes do \texorpdfstring{\ac{TSP}}{TSP}}`.
- Busca automatizada nos cinco capítulos modificados, removendo `\ac{...}`, `\acp{...}`, comandos LaTeX e blocos `\texorpdfstring{...}{...}`, retornou `bare_count 0` para remanescentes manuais de `GA`, `PSO`, `ACO`, `TSP`, `TSP-SD-ATP`, `AP`, `LB`, `CD`, `OX`, `NP`, `lower bound`, `Lower Bound` e `crossover OX`.
- A mesma checagem retornou `undefined []` para chaves de acronym indefinidas.
- A mesma checagem retornou `math_ac []`, indicando ausência de `\ac`/`\acp` em matemática nos cinco capítulos modificados.

## Required fixes

- Nenhum ajuste obrigatório para P37 nos cinco capítulos modificados e em `Abreviaturas.tex`.

## Notes

- Uma busca ampla em `monografia/**/*.tex` ainda encontra siglas manuais em arquivos fora do escopo direto de P37 nos capítulos, como resumo/abstract em `main_ppgco_ufu.tex`, tabelas geradas e figura TikZ. Esses achados não foram tratados como falha desta rechecagem porque a linha P37 limita a aplicação aos 5 capítulos e a solicitação pediu para não editar monografia nem roadmap.
