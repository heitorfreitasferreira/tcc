# Validação P43 — Held-Karp 1971 × 1962

## Verdict

PASS

## Escopo

Validar a execução do item P43 do roadmap: remover a atribuição incorreta de programação dinâmica `O(n²2ⁿ)` ao paper [[heldkarp1971traveling]] e propagar a distinção correta entre a linha Held-Karp 1970/1971 e o paper de 1962.

## Alterações Avaliadas

- `vault/areas/lower-bounds.md`: entrada de [[heldkarp1971traveling]] corrigida para ascent method + branch-and-bound, com aviso explícito de que não é a DP de 1962.
- `vault/papers/heldkarp1970traveling.md`: conexão com a Parte II corrigida para ascent method e branch-and-bound.
- `vault/writing/auditorias/validacao-resumos.md`: auditoria histórica anotada como corrigida por P42/P43, sem manter a atribuição errada como claim vigente.
- `monografia/cap_fundamentacao/fundamentacao.tex`: Seção 2.8 explicita que 1970--1971 correspondem a 1-trees, multiplicadores lagrangeanos, ascent method e branch-and-bound, enquanto a DP `O(n^2 2^n)` é de Held e Karp (1962).

## Evidências

- `bash scripts/check-monografia.sh` retornou `PASS check-monografia`.
- Busca textual por `DP O(n²2ⁿ)`, `programação dinâmica O(n²2ⁿ)`, `Held-Karp 1962` e `heldkarp1971traveling` não encontrou atribuição problemática ativa remanescente nos arquivos operacionais do vault e da monografia.
- Juiz 1 registrou `WARN` apenas porque o roadmap ainda não havia sido marcado como concluído no momento da validação; a ressalva foi resolvida nesta atualização.
- Juiz 2 registrou `PASS`, destacando que a busca semântica complementa o `check-monografia.sh` para este escopo.

## Recomendação

Marcar P43 como concluída. Se o gate anti-alucinação for expandido no futuro, considerar uma regra textual que alerte quando `heldkarp1971traveling` aparecer próximo de `DP`, `programação dinâmica`, `dynamic programming` ou `O(n²2ⁿ)` sem linguagem explícita de negação ou correção.
