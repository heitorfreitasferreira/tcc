# Verdict

PASS.

# Evidence

- `vault/areas/lower-bounds.md:92-93` agora identifica `heldkarp1970traveling` como bound HK original e `heldkarp1971traveling` como ascent method + branch-and-bound para o bound HK, com aviso explícito de que não é a DP `O(n²2ⁿ)` de Held-Karp 1962.
- `vault/papers/heldkarp1970traveling.md:56` removeu a conexão incorreta com programação dinâmica e descreve a Part II como ascent method e branch-and-bound para o bound HK.
- `vault/papers/heldkarp1971traveling.md:40-46` é a fonte de controle correta: 1971 continua a série 1-tree/lower bound, apresenta ascent method e branch-and-bound, e remete a DP `O(n²2^n)` ao artigo separado de 1962, `A dynamic programming approach to sequencing problems`.
- `vault/writing/auditorias/validacao-resumos.md:126`, `:146`, `:179` e `:227` preserva o histórico do erro, mas marca a auditoria anterior como corrigida e atribui a DP a 1962, não a 1971.
- `monografia/cap_fundamentacao/fundamentacao.tex:92` preserva a distinção acadêmica exigida: 1970--1971 = 1-trees, multiplicadores lagrangeanos, ascent method e branch-and-bound; 1962 = programação dinâmica `O(n^2 2^n)` para problemas de sequenciamento.
- Buscas textuais por combinações de `Held-Karp`, `1971`, `programação dinâmica`, `dynamic programming`, `DP`, `O(n²2ⁿ)`, `2^n` e variantes não encontraram atribuição problemática ativa nos arquivos de escopo.
- As ocorrências remanescentes em `vault/writing/planejamento/roadmap-monografia.md` documentam o item P43 e a correção esperada; a ocorrência em `.agents/council/2026-06-04-validate-referencial-judge-tsp-otimizacao.md:157` é relatório histórico anterior à correção e não é fonte de monografia/vault operacional.
- `bash scripts/check-monografia.sh` executou com `PASS metrics`, `PASS monografia`, `PASS review-requests` e `PASS check-monografia`.

# Risks

- `check-monografia.sh` não é suficiente sozinho para este escopo: ele valida consistência estrutural da monografia, citações, figuras, artefatos gerados, métricas hardcoded e reviews pendentes, mas não verifica semanticamente a atribuição Held-Karp 1971 vs. 1962.
- O teste adicional necessário para P43 é a busca textual semântica executada neste parecer, combinada com leitura dos arquivos de escopo e da nota de controle `vault/papers/heldkarp1971traveling.md`.
- Permanece ruído histórico em relatórios/roadmap, mas ele está fora do escopo operacional e, nos arquivos de planejamento, aparece como descrição do problema P43, não como claim técnico vigente.

# Recommendation

Manter P43 como concluído. Para evitar regressão futura, se houver expansão do gate anti-alucinação, adicionar uma checagem textual simples que falhe quando `heldkarp1971traveling` aparecer próximo de `DP`, `programação dinâmica`, `dynamic programming` ou `O(n²2ⁿ)` sem linguagem explícita de negação/correção.
