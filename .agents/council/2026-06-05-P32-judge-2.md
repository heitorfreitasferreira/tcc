## Judge 2 Verdict

### A. Code-Documentation Alignment: PASS

Reasoning: Todas as afirmações do `.tex` são respaldadas pelo código `ant.go` e `main.go`:

| Afirmação no LaTeX (linha) | Evidência no código |
|---|---|
| "todas as formigas depositam feromônio ao final de cada iteração" (74) | `ant.go:28` — `for _, ant := range ants` itera sobre todas as formigas sem filtro de `iteration-best` |
| "proporcionalmente ao inverso do custo da rota construída" (74) | `ant.go:32` — `delta := aco.Q / ant.lk` |
| "feromônio tridimensional $\tau_{i,j,k}$" (76) | `main.go:21` — `pheromones [][][]float64`; `main.go:86-96` aloca tensor 3D; `ant.go:33-37` deposita em `pheromones[prev][curr][next]`; `ant.go:54` consulta `pheromones[prev][curr][next]` |
| "informação heurística $\eta_{i,j,k}=1/c_{i,j,k}$" (76) | `ant.go:53` — `heuristic := 1.0 / aco.Graph[prev][curr][next]` |
| Evaporação global (1−ρ)·τ (implícito no AS) | `ant.go:22` — `aco.pheromones[i][j][k] *= (1 - aco.Rho)` sobre toda a matriz |

O código implementa Ant System puro com tensor 3D de feromônio, sem bounds (não é MMAS) — exatamente o que o parágrafo descreve. A correção do P27 (linha 74: "baseia-se no Ant System original") foi verificada e está consistente com a nota `stutzle2000mmas.md` (que agora documenta corretamente que MMAS não está implementado).

### B. Claim Strength vs. Evidence: WARN

**Acertos:**
- "invariavelmente representado como uma matriz bidimensional $\tau_{i,j}$" (linha 78): respaldado pela verificação de 84 papers do vault + surveys (dorigo2004book, misra2024acorecent). Afirmação forte e correta.
- "adaptação necessária para capturar a dependência de segunda ordem" (linha 78): justificada pela estrutura do TSP-SD-ATP (tensor de custo $G[i][j][k]$ com dependência de 2ª ordem), embora "necessária" seja ligeiramente forte — alternativas como expansão de grafo (turn-cost routing, winter2002modeling) existem. "Natural" ou "motivada pela" seria mais preciso, mas não é incorreto.
- Qualificação final: "não foi encontrada na literatura consultada" (linha 78) — hedge honesto.

**Problema: omissão de Starzec 2026.**
A nota `feromonio-3d-originalidade.md` documenta 4 precedentes (Wang 2013, Wang 2015, MDACO 2025, Starzec 2026), mas o parágrafo do LaTeX cita apenas 3. Starzec 2026 (DOI: `10.58032/agh/3gokaq`) aplica feromônio multidimensional ao MOTSP — é o precedente mais próximo do TCC porque está no domínio TSP (multi-objetivo). Os três citados são todos de domínios não-TSP (task allocation, network coding, VRP). Omitir Starzec 2026 cria um viés sutil: o leitor vê precedentes apenas em domínios distantes e conclui que feromônio multidimensional é inédito em TSP, o que Starzec 2026 contradiz. Ainda que a semântica seja diferente (MOTSP vs. Markov de 2ª ordem), o paper deveria ser ao menos mencionado e diferenciado.

### C. Search Methodology Adequacy: WARN

**Acertos:**
- Metodologia bem documentada em `feromonio-3d-originalidade.md`, com tabela de fontes, status e limitações.
- 84 papers do vault verificados — cobertura completa da literatura canônica de ACO.
- 7 termos de busca específicos e bem escolhidos.
- Áreas de risco explicitamente listadas (scheduling, VRPTW, TDTSP, hyper-heuristics).

**Problema: gap de scheduling não investigado.**
A própria nota classifica "ACO para scheduling com 3 índices" como risco **"Baixa" confiança** com nota **"risco real"** (linha 106). O dorigo2004book lista scheduling como domínio de aplicação de ACO. Se algum paper usa feromônio $\tau(m, j, p)$ para máquina × job × posição, isso seria um precedente estrutural para $\tau(i,j,k)$. Os termos de busca listados são todos TSP-cêntricos; nenhum cobre `"ant colony" scheduling three-index pheromone` ou `"ant colony" job shop pheromone`. Esta avenida não foi investigada apesar de identificada como risco.

**Adicional:** Google Scholar bloqueado por CAPTCHA, Semantic Scholar rate-limited, DBLP indisponível, arXiv timeout — múltiplas falhas de API que limitaram a busca. Apesar de bem documentadas, estas limitações reduzem a confiança da afirmação de que "não foi encontrada na literatura consultada."

### D. Missing Precedent: WARN

**Gap primário: Starzec 2026 ausente do LaTeX e do vault.**
- A nota `feromonio-3d-originalidade.md` (linhas 118-125) lista Starzec & Starzec (2026) como 4º precedente com DOI `10.58032/agh/3gokaq`, domínio "Multi-objective TSP".
- Não existe nota `vault/papers/starzec*.md` — o vault não foi populado.
- Não existe entrada BibTeX para este paper.
- O LaTeX cita apenas 3 dos 4 precedentes conhecidos.
- Starzec 2026 é o precedente **mais próximo do domínio do TCC** (MOTSP é TSP multi-objetivo, enquanto os outros três são task allocation, network coding e VRP). Sua omissão é a mais significativa porque enfraquece a honestidade acadêmica do parágrafo.

**Gap secundário: ausência de disclosure de limitações de busca no LaTeX.**
O parágrafo usa "na literatura consultada" como único hedge, sem indicar escopo, fontes ou limitações da busca. Uma nota de rodapé mencionando as APIs bloqueadas (Google Scholar CAPTCHA, Semantic Scholar rate-limit) e as áreas não cobertas (scheduling) fortaleceria a transparência acadêmica.

### E. Implementation Correctness: PASS

**BibTeX keys:**
- `\cite{wang2013aco}` → `@article{wang2013aco,` ✓
- `\cite{wang2015aco}` → `@article{wang2015aco,` ✓
- `\cite{geng2025mdaco}` → `@inproceedings{geng2025mdaco,` ✓

Todos os três comandos `\cite{}` no `.tex` (linha 78) correspondem exatamente às chaves BibTeX. Tipos de entrada corretos (article para periódicos, inproceedings para CCDC 2025).

**Vault notes:**
| Nota | Status | Rating | Faz overclaim? |
|---|---|---|---|
| `wang2013aco.md` | `nao-lido` | 2 | Não — resume como "precedente de feromônio 3D em ACO (domínio: task allocation, não TSP)" |
| `wang2015aco.md` | `nao-lido` | 3 | Não — resume como "precedente de feromônio multidimensional... Domínio: network coding, não TSP" |
| `geng2025mdaco.md` | `nao-lido` | 2 | Não — diz "precedente mais próximo estruturalmente" mas corretamente limita a "VRP, não TSP" |

Todas as três notas são honestas: marcadas como `nao-lido`, ratings baixos, e afirmam explicitamente que os domínios são diferentes do TCC. Nenhuma faz afirmações além do que é conhecido. O ano no BibTeX de `wang2015aco` é 2016 (ano de publicação do volume 20(3) do IEEE TEVC), enquanto a chave usa 2015 (ano do DOI/online first) — prática comum e não é erro.

**P27 verificado:** `stutzle2000mmas.md` linha 31 agora diz "O código implementa Ant System (todas as formigas depositam, sem bounds), e não MMAS" — a correção foi aplicada.

## Overall: WARN

O trabalho está fundamentalmente correto: o código implementa exatamente o que o LaTeX descreve, as chaves BibTeX batem, as notas do vault são honestas, e o claim é qualificado (não absoluto). No entanto, há três lacunas que precisam ser endereçadas antes que o parágrafo esteja pronto para defesa:

## Required Fixes (if any)

1. **[MUST] Adicionar Starzec 2026 ao parágrafo e ao BibTeX.** Criar `vault/papers/starzec2026motsp.md`, entrada BibTeX, e citar no LaTeX (linha 78) como 4º precedente — ou explicitar por que não é citado (ex: artigo não revisado por pares, ou feromônio multidimensional com semântica diferente). A omissão atual, dado que Starzec está no domínio TSP, é a lacuna mais séria.

2. **[SHOULD] Investigar o gap de scheduling ou documentar a limitação.** A nota de originalidade classifica ACO para scheduling com 3 índices como "risco real" com "Baixa" confiança. Buscar explicitamente por `"ant colony" scheduling pheromone three-index` ou documentar no LaTeX que scheduling não foi coberto pela busca.

3. **[SHOULD] Adicionar nota de rodapé sobre limitações da busca.** Incluir algo como: "A busca abrangeu 84 artigos do referencial, a API OpenAlex (6.993 resultados) e o CrossRef; APIs como Google Scholar e Semantic Scholar estavam indisponíveis por rate-limiting no momento da consulta."

4. **[NICE] Suavizar "necessária" → "natural" ou "motivada pela".** A palavra "necessária" implica que não há alternativa, mas abordagens como expansão de grafo (turn-cost routing) poderiam modelar a dependência sem feromônio 3D. "Adaptação natural" ou "motivada pela estrutura do TSP-SD-ATP" é mais preciso academicamente.

5. **[NICE] Criar notas de vault para os 3 papers citados com status ≥ "resumo-lido".** Atualmente todas as três notas estão `nao-lido`. Ler ao menos os abstracts completos (via DOI) e atualizar o status para garantir que as afirmações do LaTeX sobre estes precedentes são precisas.
