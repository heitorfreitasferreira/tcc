# Judge 1 — Verdict: P17 "Triagem de PDFs Faltantes"

**Veredito:** APPROVE — com 1 correção obrigatória e 2 observações menores.

---

## 1. Consistência com o Roadmap

Roadmap (lines 130–137) vs. P17 — mapeamento perfeito:

| Categoria Roadmap | Ref count | P17 `classificacao` | Match |
|---|---|---|---|
| Download manual prioritário | 3 | `download-manual-prioritario` | ✓ |
| Condicionado a lower bounds | 5 | `condicionado-lower-bounds` | ✓ |
| Análise posterior | 4 | `analise-posterior` | ✓ |
| Candidatas a descarte | 5 | `descartado` | ✓ |
| (deepaco2023 foi recuperado) | 1 | (sem `classificacao`, `status: disponível`) | ↑ melhor que o roadmap |

O roadmap agrupava `deepaco2023` em "análise posterior"; o P17 o recuperou, um desvio positivo. As demais 17 referências seguem exatamente a pré-classificação do roadmap.

---

## 2. Valores de `status` — sensatos e uniformes

Todos os 4 valores usados são claros e auto-descritivos:

- `disponível` — 1 nota (deepaco2023, PDF íntegro)
- `solicitar-download-manual` — 3 notas (lawler, aggarwal, vanhove)
- `analise-posterior` — 9 notas (heldkarp, balas, fischetti, valenzuela, leraromero, dellamico2021, dellamico2022, ahmed, nagata)
- `descartado-escopo-imediato` — 5 notas (hga, sun, huang, kappagantula, toaza)

Os valores de `status` e `classificacao` são mutuamente consistentes em todas as 17 notas que possuem ambos os campos.

---

## 3. DeepACO PDF — válido

- `pdfinfo`: 23 páginas, 1.1 MB, PDF 1.5, sem criptografia
- `pdftotext`: texto extraível, estrutura de paper NeurIPS com Abstract + Introduction legíveis
- Arquivo substituiu versão anterior corrompida
- **PDF íntegro e utilizável** ✓

---

## 4. Correção obrigatória

### 4.1 Erro de contagem no documento de triagem (linha 29)

A tabela "Resultado Global" diz `Análise posterior | 10`, mas o somatório das sub-tabelas é:
- Condicionados a lower bounds: 5
- Trabalhos relacionados / futuros: 4
- **Total: 9**

O número correto é **9**. O total de notas modificadas é 18 (1 + 3 + 9 + 5), o que bate com a descrição da task. Corrigir "10" → "9" na linha 29 do documento de triagem.

---

## 5. Observações menores (não bloqueantes)

### 5.1 `deepaco2023.md` não tem campo `classificacao`

As outras 17 notas incluíram `classificacao`; deepaco2023 só tem `status: disponível`. Por consistência, poderia receber `classificacao: recuperado`. Não bloqueante — o status já é auto-explicativo.

### 5.2 `vanhove2012route.md` tem `rating: 0`

Referência central para turn costs com rating 0 sugere que nunca foi avaliada. Problema pré-existente, não introduzido pelo P17. Recomendar revisão quando/se o PDF for obtido.

### 5.3 `heldkarp1971traveling.md` — tags com vírgula

Tags `[tsp, lower-bound, lagrangean]` usam vírgula como separador, enquanto as demais notas usam espaço. Inconsistência cosmética, não introduzida pelo P17.

---

## 6. Verificação cruzada: diretório `corrupted/`

Os 4 arquivos listados no documento de triagem (seção "PDFs Corrompidos Residuais") conferem com o diretório:

```
dellamico2021multiple.pdf  (3.7 MB, análise posterior)
dellamico2022exact.pdf     (596 KB, análise posterior)
nagata2006eax.pdf          (898 B,  análise posterior)
vanhove2012.pdf            (815 KB, solicitar download manual)
```

O nagata2006eax.pdf com 898 bytes é suspeito (provavelmente um HTML de erro salvo como PDF). Os demais são arquivos substanciais mas corrompidos. Classificações consistentes com o status das respectivas notas.

---

## Conclusão

A triagem foi executada com rigor metodológico: 3 fontes de download tentadas, classificação editorial alinhada ao roadmap, documentação clara dos resultados. O único defeito material é o erro de contagem na tabela-resumo (10 → 9). Com essa correção, o P17 está aprovado.
