# Validacao independente do P45 - juiz 2

Veredito: WARN

## Escopo validado

- Roadmap lido em `vault/writing/planejamento/roadmap-monografia.md`, especialmente a secao de conciliacao entre `vault/papers/` e `vault/papers/summaries/` e o item P45.
- Auditoria lida em `vault/writing/auditorias/comparacao-papers-summaries.md`.
- Amostras pareadas lidas em `vault/papers/` e `vault/papers/summaries/`: `agatz2018optimization`, `aggarwal2000angular`, `balas1985branch`, `chandra2022comparative`, `heldkarp1971traveling`, `hossain2024comparison`, `lysgaard1999cluster`, `muthanna2022uav`, `rajwar2023exhaustive` e `valenzuela1997estimating`.
- Nao alterei notas canonicas nem arquivos do vault fora deste relatorio.

## Evidencias de conformidade

- O P45 exige comparar cada nota pareada, sem editar notas canonicas, documentando trechos mais completos de `summaries/`, campos faltantes em `vault/papers/`, divergencias, necessidade de validacao no PDF e recomendacoes preliminares. Isso esta explicito no roadmap em `vault/writing/planejamento/roadmap-monografia.md:187` e `vault/writing/planejamento/roadmap-monografia.md:485`.
- A auditoria declara o escopo correto: 49 notas em `vault/papers/summaries/*.md`, nenhum par sem nota canonica, e decisao de nao incorporar conteudo no P45 em `vault/writing/auditorias/comparacao-papers-summaries.md:24-27`.
- Conferencia independente de contagem: existem 49 summaries, a auditoria tem 49 secoes `###` dentro de `## Inventario por paper`, nao ha summary sem secao, nao ha secao extra, e nenhum summary ficou sem nota canonica pareada.
- A auditoria inclui uma secao por paper com formato consistente: conteudo mais completo nos summaries, avaliacao da nota canonica, campos faltantes, divergencia, validacao no PDF e recomendacao preliminar. Exemplos aparecem em `agatz2018optimization` em `vault/writing/auditorias/comparacao-papers-summaries.md:46-53`, `balas1985branch` em `vault/writing/auditorias/comparacao-papers-summaries.md:100-107` e `valenzuela1997estimating` em `vault/writing/auditorias/comparacao-papers-summaries.md:442-449`.
- A sintese de campos faltantes bate com uma checagem independente do frontmatter das 49 notas pareadas: `pdf_status` ausente em 41, `chapters` em 48, `claim_support` em 48, `aliases` em 46, `areas` em 31 e `methods` em 9. Esses numeros conferem com `vault/writing/auditorias/comparacao-papers-summaries.md:34`.
- As prioridades altas para notas canonicas praticamente sem corpo sao justificadas por amostras: `vault/papers/aggarwal2000angular.md` termina no frontmatter em `line 41`, `vault/papers/balas1985branch.md` termina no frontmatter em `line 34`, e `vault/papers/lysgaard1999cluster.md` termina no frontmatter em `line 33`, enquanto os respectivos summaries contem secoes analiticas completas.
- A auditoria preserva `vault/papers/` como destino canonico e trata `summaries/` como fonte auxiliar, em alinhamento com `vault/writing/planejamento/roadmap-monografia.md:181`.

## Problemas encontrados

- Ha divergencias de DOI nao inventariadas. Em `aggarwal2000angular`, a nota canonica traz `10.1137/S0097539796312719` em `vault/papers/aggarwal2000angular.md:15`, enquanto o summary traz `10.1137/S0097539796312721` em `vault/papers/summaries/aggarwal2000angular.md:5`; a auditoria afirma que nao ha divergencia automatica relevante em `vault/writing/auditorias/comparacao-papers-summaries.md:60`.
- Ha outra divergencia de DOI nao registrada em `muthanna2022uav`: a nota canonica traz `10.1016/j.comcom.2022.04.028` em `vault/papers/muthanna2022uav.md:15`, enquanto o summary traz `10.1016/j.comcom.2022.04.029` em `vault/papers/summaries/muthanna2022uav.md:4`; a auditoria registra apenas divergencia de heading/titulo em `vault/writing/auditorias/comparacao-papers-summaries.md:366`.
- Algumas divergencias de titulo parecem falso positivo por quebra de linha YAML. Em `hossain2024comparison`, o titulo canonico continua em `vault/papers/hossain2024comparison.md:2-3`, mas a auditoria diz que parece truncado em `Large-Scale` em `vault/writing/auditorias/comparacao-papers-summaries.md:294`.
- O mesmo ocorre em `rajwar2023exhaustive`: o titulo canonico continua em `vault/papers/rajwar2023exhaustive.md:2-3`, mas a auditoria diz que termina em `Open` em `vault/writing/auditorias/comparacao-papers-summaries.md:418-421`.
- A checagem de divergencias parece ter sido feita de forma mais superficial para metadados do que para estrutura. O P45 cita explicitamente conflitos de autoria, ano, DOI e resultados como casos que exigem validacao no PDF no roadmap em `vault/writing/planejamento/roadmap-monografia.md:190-191`; portanto os DOIs divergentes deveriam estar no inventario.

## Riscos residuais

- P46 pode desperdicar prioridade validando titulos que nao estao truncados (`hossain2024comparison`, `rajwar2023exhaustive`) se a auditoria for consumida literalmente.
- P46 pode deixar passar conflitos bibliograficos reais se confiar nas linhas que dizem `nenhuma divergencia automatica relevante detectada`, especialmente em `aggarwal2000angular`.
- A auditoria e adequada como mapa de trabalho, mas nao deve ser usada como fonte final de correcao de metadados sem uma segunda validacao de DOI/titulo/PDF.
- Como a validacao foi amostral para conteudo substantivo, ainda pode haver outros falsos positivos ou falsos negativos em resultados numericos, autoria, ano ou relevancia para o TCC.

## Conclusao

O P45 cumpre a saida estrutural esperada: ha 49 secoes para 49 summaries, todos pareados com notas canonicas, e a auditoria nao incorpora conteudo nas notas canonicas. O veredito e WARN porque a auditoria apresenta falhas pontuais mas importantes em divergencias de metadados: omite DOIs conflitantes e marca como truncados alguns titulos que apenas estao quebrados em YAML. Recomendo aceitar P45 como inventario inicial, mas exigir em P46 uma etapa previa de saneamento das divergencias de DOI/titulo antes de qualquer incorporacao.
