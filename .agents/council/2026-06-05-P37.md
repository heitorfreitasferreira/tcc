# Council P37 - Substituir siglas manuais por `\ac{}`

## Verdict

PASS

## Packet

- Roadmap item: P37, `Substituir siglas manuais por \ac{} nos capítulos`.
- Escopo executado: cinco capítulos da monografia (`introducao`, `fundamentacao`, `proposta`, `experimentos`, `conclusao`).
- Objetivo: trocar siglas manuais por comandos do pacote `acronym`, usando as chaves definidas em `monografia/abrev/Abreviaturas.tex`.
- Validações locais: `bash scripts/check-monografia.sh` PASS; ciclo `pdflatex + bibtex + pdflatex + pdflatex` gerou PDF; recompilação após correção do council também gerou PDF.

## Judges

- Judge 1: PASS em `.agents/council/2026-06-05-P37-judge-1.md`.
- Judge 2: PASS em `.agents/council/2026-06-05-P37-judge-2.md`.

## Consolidated Findings

- P37 foi cumprido nos cinco capítulos.
- As siglas principais (`GA`, `PSO`, `ACO`, `TSP`, `TSP-SD-ATP`, `AP`, `LB`, `CD`, `OX`, `NP`, `VANT`) foram convertidas para `\ac{}`/`\acp{}` em prosa ou mantidas apenas como texto alternativo PDF dentro de `\texorpdfstring`.
- Headings com acrônimos foram protegidos com `\texorpdfstring`, evitando warnings novos de `hyperref` por `\ac` em strings PDF.
- Não foram encontrados usos de `\ac`, `\acp` ou `\acs` em modo matemático.
- Todas as chaves usadas nos capítulos existem em `monografia/abrev/Abreviaturas.tex`.

## Corrections During Council

- A primeira rodada teve PASS do juiz 1 e FAIL do juiz 2 por dois títulos com `Lower Bound` manual.
- Correção aplicada antes da rechecagem:
  - `monografia/cap_proposta/proposta.tex`: título alterado para `\texorpdfstring{\ac{LB}}{Lower Bound} por Relaxação \texorpdfstring{\ac{AP}}{AP}`.
  - `monografia/cap_experimentos/experimentos.tex`: subtítulo alterado para `\texorpdfstring{\ac{LB}}{Lower Bound} \texorpdfstring{\ac{AP}}{AP}`.
- A segunda rodada retornou PASS/PASS.

## Residual Risks

- Há siglas manuais fora dos cinco capítulos, por exemplo em resumo/abstract, tabelas geradas e figuras TikZ. Elas foram consideradas fora do escopo direto de P37, que especifica capítulos.
- A compilação ainda mostra warnings preexistentes do template/acronym/BibTeX; eles não bloqueiam P37 e `scripts/check-monografia.sh` passou.

## Required Fixes

- Nenhuma correção obrigatória restante para P37.

## Roadmap Sync After Council

- Após o PASS/PASS, `vault/writing/planejamento/roadmap-monografia.md` foi atualizado para marcar P37 como `Concluída`.
- Referências operacionais obsoletas foram sincronizadas: P37 removido da lista de pendências abertas e a recomendação de P46 removida por já estar concluída.
