# Verdict

WARN

# Checks

- Li o contexto de P42/P43 em `vault/writing/planejamento/roadmap-monografia.md`.
- Conferi `vault/papers/heldkarp1971traveling.md`: a nota atribui corretamente o paper de 1971 a *ascent method* + *branch-and-bound* para o lower bound Held-Karp e explicita que a DP `O(n²2^n)` pertence ao artigo separado de 1962.
- Conferi `vault/areas/lower-bounds.md`: a entrada de referência em linha 93 foi corrigida para "ascent method + branch-and-bound para o bound HK; não é a DP O(n²2ⁿ) de Held-Karp 1962".
- Conferi `vault/papers/heldkarp1970traveling.md`: a relação com `heldkarp1971traveling` está descrita como Parte II com ascent method e branch-and-bound, sem repetir a atribuição errada de DP.
- Conferi `vault/writing/auditorias/validacao-resumos.md`: as menções remanescentes registram explicitamente que a auditoria antiga estava incorreta e que a atribuição foi corrigida por P42/P43.
- Conferi `monografia/cap_fundamentacao/fundamentacao.tex`: a Seção 2.8 distingue corretamente a linha Held-Karp 1970--1971 da formulação por programação dinâmica de Held e Karp (1962).
- Busquei ocorrências de `DP O(n²2ⁿ)`, `programação dinâmica O(n²2ⁿ)`, `Held-Karp 1962` e `heldkarp1971traveling` em `vault/`, `monografia/` e no repositório.

# Findings

- Não encontrei ocorrência ativa que atribua a DP `O(n²2ⁿ)` ao paper `heldkarp1971traveling` como claim factual vigente.
- As ocorrências remanescentes em `validacao-resumos.md` e no roadmap são registros históricos ou instruções de correção, e indicam explicitamente que a atribuição anterior estava errada.
- A monografia ficou substantivamente correta para este ponto: cita `heldkarp1970traveling` e `heldkarp1971traveling` como referências de relaxação/lower bound por 1-trees, multiplicadores lagrangeanos, ascent method e branch-and-bound; separa a DP de 1962.
- Há um problema processual: `vault/writing/planejamento/roadmap-monografia.md:483` ainda marca P43 como `Pendente (alta)`, e as linhas 580--582 ainda tratam P43 como próxima pendência. Isso não indica erro técnico no conteúdo corrigido, mas impede considerar o roadmap operacionalmente sincronizado.

# Recommendation

- Marcar P43 como concluída após atualizar o status no roadmap e remover/ajustar a lista de pendências finais que ainda apresenta P43 como aberta.
- Não é necessário alterar `vault/areas/lower-bounds.md`, `vault/papers/heldkarp1971traveling.md` nem `monografia/cap_fundamentacao/fundamentacao.tex` para este item.
