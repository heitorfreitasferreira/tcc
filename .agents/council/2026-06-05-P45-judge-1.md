# Validacao P45 — Judge 1

Data: 2026-06-05

Veredito: **WARN**

## Escopo Validado

- Item do roadmap: `vault/writing/planejamento/roadmap-monografia.md`, P45.
- Arquivo auditado: `vault/writing/auditorias/comparacao-papers-summaries.md`.
- Amostras lidas em `vault/papers/` e `vault/papers/summaries/`: `aggarwal2000angular`, `balas1985branch`, `lysgaard1999cluster`, `heldkarp1971traveling`, `hossain2024comparison`, `rajwar2023exhaustive`, `nagata2006eax`, `valenzuela1997estimating`.
- Restricao conferida: nenhuma nota canonica foi editada nesta validacao.

## Resultado da Conferencia

- `vault/papers/summaries/` contem **49** arquivos `.md`.
- A auditoria contem **49** secoes `###` sob `## Inventario por paper`.
- Nao ha `summary` sem secao correspondente na auditoria.
- Nao ha secao de auditoria sem arquivo correspondente em `summaries/`.
- Nao ha `summary` sem nota canonica correspondente em `vault/papers/`.
- O arquivo novo cumpre a forma principal exigida por P45: inventario separado por paper, sem alteracao das notas canonicas, com campos faltantes, divergencias, validacao no PDF e recomendacoes preliminares.

## Achados

- **Cobertura estrutural passa.** A comparacao automatica de nomes confirmou 49 summaries, 49 secoes e correspondencia exata entre os conjuntos de nomes.
- **Formato por paper passa.** Cada secao amostrada segue o padrao esperado: diferenca de completude entre `summaries/` e nota canonica, campos faltantes, divergencia, indicacao de validacao no PDF e recomendacao preliminar.
- **Preservacao das notas canonicas passa.** A auditoria declara que nao decidiu incorporacao e orienta P46 a fazer decisao assistida; a validacao nao encontrou edicoes necessarias em `vault/papers/` para completar P45.
- **Amostras de notas quase vazias foram bem identificadas.** `aggarwal2000angular`, `balas1985branch` e `lysgaard1999cluster` realmente possuem pouco ou nenhum corpo estruturado na nota canonica, enquanto os summaries trazem conteudo analitico substancial; a prioridade alta indicada e coerente.
- **Amostras de notas corrigidas recentemente foram tratadas com cautela.** `heldkarp1971traveling` e `nagata2006eax` preservam correcoes recentes nas notas canonicas; a auditoria corretamente recomenda nao substituir a nota canonica e validar contra PDF antes de migrar detalhes.
- **Divergencia material nao inventariada em `aggarwal2000angular`.** A nota canonica registra DOI `10.1137/S0097539796312719`, enquanto o summary registra `10.1137/S0097539796312721`. A secao da auditoria diz que nao ha divergencia automatica relevante. Como P45 exige divergencias e validacao de DOI, isso deve ser corrigido em P46 ou em uma revisao curta da auditoria.
- **Divergencia material pouco explicitada em `balas1985branch`.** A nota canonica usa ano `1985`, enquanto o summary declara ano `1983` para o report MSRR 488. A auditoria menciona falta de DOI e validacao bibliografica, mas afirma que nao ha divergencia automatica relevante. O ano/versao deveria estar listado como divergencia a validar no PDF ou na fonte bibliografica.
- **Possivel falso positivo em titulos quebrados por YAML.** `hossain2024comparison` e `rajwar2023exhaustive` foram marcados como titulo truncado, mas as notas canonicas mostram titulo completo em YAML quebrado em duas linhas. A recomendacao de validar no PDF e segura, mas a divergencia parece mais de formatacao/line wrap do que truncamento real.
- **Achado incidental fora do nucleo de P45.** `valenzuela1997estimating` tem um campo `areas` aparentemente malformado (`- tsp, "lower-bounds"]`), embora a auditoria classifique campos minimos como presentes. Isso nao invalida P45, mas deve entrar em limpeza de schema se houver etapa posterior de normalizacao do vault.

## Recomendacoes

- Manter P45 como concluivel com ressalvas: o entregavel existe, cobre todos os pares e e utilizavel como base para P46.
- Antes de incorporar conteudo em P46, revisar manualmente as divergencias bibliograficas de `aggarwal2000angular` e `balas1985branch`, especialmente DOI, ano, versao de publicacao e chave BibTeX.
- Em P46, tratar marcacoes de titulo truncado com parsing visual do frontmatter/YAML antes de corrigir metadados, para evitar alterar titulos que ja estao completos.
- Registrar no proprio fluxo de P46 que diferencas de DOI, ano, autoria, titulo e resultados numericos exigem validacao no PDF ou fonte bibliografica primaria antes de qualquer atualizacao canonica.
- Considerar uma etapa separada de limpeza de schema para campos malformados ou inconsistentes, sem misturar essa tarefa com a incorporacao de conteudo dos summaries.

## Conclusao

P45 deve receber **WARN**: o objetivo operacional foi substancialmente cumprido, com cobertura completa e boa organizacao, mas a amostragem encontrou divergencias bibliograficas relevantes que a auditoria nao inventariou explicitamente. Isso nao bloqueia o uso do arquivo como inventario preliminar, desde que P46 comece pelos casos bibliograficos de maior risco e valide DOI/ano/titulo no PDF antes de alterar notas canonicas.
