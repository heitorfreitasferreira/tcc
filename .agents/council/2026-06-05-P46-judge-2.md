# Validacao independente do P46 - juiz 2

Veredito: WARN

## Escopo validado

- Revisei apenas os arquivos indicados como pacote P46.
- Conferi o alinhamento entre notas canonicas, `monografia/bib/abntex2-references.bib`, `vault/papers/index.md` e as auditorias `comparacao-papers-summaries.md` e `validacao-resumos.md`.
- Considerei as validacoes ja executadas pelo executor: `safe_load` do YAML e `bash scripts/check-monografia.sh` com `PASS`.
- Nao avaliei outras mudancas pre-existentes do worktree.

## Achados

- WARN acionavel antes de marcar o roadmap: `deepaco2023` esta bibliograficamente inconsistente entre a nota canonica e o BibTeX. A nota registra titulo `DeepACO: Neural-enhanced Ant Systems for Combinatorial Optimization`, autores `Haoran Ye`, `Jiarui Wang`, `Zhiguang Cao`, `Helan Liang`, `Yong Li` e DOI `10.48550/arXiv.2305.19416` em `vault/papers/deepaco2023.md:2-15`; o BibTeX registra titulo diferente, autores diferentes e arXiv `2309.14032` em `monografia/bib/abntex2-references.bib:338-342`. Como `deepaco2023` e citado na monografia em `monografia/cap_conclusao/conclusao.tex:28`, essa divergencia deve ser resolvida antes de encerrar P46.
- WARN acionavel antes de marcar o roadmap: `lysgaard1999cluster` foi enriquecido e marcado com `chapters: fundamentacao` em `vault/papers/lysgaard1999cluster.md:34-35`, mas nao aparece em `vault/papers/index.md` nem na lista de PDFs integros ou pendencias BibTeX. A busca por `lysgaard1999cluster` no indice nao retornou ocorrencias, enquanto a nota existe e tem PDF em `vault/papers/lysgaard1999cluster.md:8-10`. Isso torna o catalogo inconsistente com a incorporacao P46.
- WARN acionavel antes de marcar o roadmap: `balas1985branch` e `lysgaard1999cluster` nao possuem entradas em `monografia/bib/abntex2-references.bib`; a busca por chaves no BibTeX encontrou apenas `deepaco2023` e `heldkarp1971traveling` entre as chaves conferidas. `balas1985branch` ao menos aparece em `Pendencias BibTeX` em `vault/papers/index.md:146-150`, mas `lysgaard1999cluster` nao aparece nessa pendencia. Se essas notas permanecerem com `chapters: fundamentacao`, o estado bibliografico precisa ficar explicito no indice ou receber entrada BibTeX.
- WARN de schema/processo: as notas que P46 declara como validadas ou corrigidas continuam com `validation_status: requer-validacao` ou `validation_status: nao-validado`. Exemplos: `aggarwal2000angular` em `vault/papers/aggarwal2000angular.md:38`, `balas1985branch` em `vault/papers/balas1985branch.md:33`, `lysgaard1999cluster` em `vault/papers/lysgaard1999cluster.md:32`, `muthanna2022uav` em `vault/papers/muthanna2022uav.md:43`, e as notas seletivamente incorporadas em `deepaco2023`, `dellamico2021multiple`, `dellamico2022exact`, `wang2021ant` e `vanhove2012route`. Isso nao quebra YAML, mas contradiz a auditoria P46 quando ela afirma confirmacao por PDF em `vault/writing/auditorias/comparacao-papers-summaries.md:50-60`.
- WARN menor: `vault/papers/index.md` esta parcialmente stale em `Pendencias BibTeX`. Ele ainda lista `heldkarp1971traveling` como pendencia em `vault/papers/index.md:153`, embora a entrada BibTeX exista em `monografia/bib/abntex2-references.bib:690-699`. Isso sugere que a contagem `Com BibTeX na monografia | 50` e a tabela de pendencias nao foram recalculadas integralmente apos P46.
- PASS com ressalva: nao encontrei promocao indevida de claims fortes nas incorporacoes seletivas principais. `dellamico2021multiple`, `dellamico2022exact`, `wang2021ant` e `vanhove2012route` preservam linguagem condicional para numeros ou limiares em `vault/papers/dellamico2021multiple.md:55-63`, `vault/papers/dellamico2022exact.md:56-64`, `vault/papers/wang2021ant.md:55-63` e `vault/papers/vanhove2012route.md:53-61`. `chandra2022comparative` tambem registra limitacao de uso em `vault/papers/chandra2022comparative.md:101-102`, embora a frase de relevancia em `vault/papers/chandra2022comparative.md:70` seja um pouco forte ao dizer que o ranking fornece referencia para expectativas de desempenho.
- PASS: as correcoes de DOI/ano/titulo pedidas para `aggarwal2000angular`, `muthanna2022uav`, `heldkarp1971traveling` e `chandra2022comparative` estao refletidas nas notas e, quando ha entrada BibTeX correspondente, no BibTeX. Exemplos: `aggarwal2000angular` usa DOI `10.1137/S0097539796312721` e chave `aggarwal1999angular` em `vault/papers/aggarwal2000angular.md:14-17` e `monografia/bib/abntex2-references.bib:199-208`; `muthanna2022uav` usa DOI `10.1016/j.comcom.2022.04.029` em `vault/papers/muthanna2022uav.md:14-15`; `heldkarp1971traveling` preserva DOI `10.1007/BF01584070` em `vault/papers/heldkarp1971traveling.md:8-9` e `monografia/bib/abntex2-references.bib:690-699`; `chandra2022comparative` usa o titulo sem `Problem` em `vault/papers/chandra2022comparative.md:2` e `monografia/bib/abntex2-references.bib:653-661`.

## Riscos residuais

- As validacoes automatizadas executadas cobrem sintaxe YAML e integridade geral da monografia, mas nao detectam divergencia entre frontmatter de nota e BibTeX, nem stale entries no indice.
- Sem corrigir `deepaco2023`, a monografia pode citar uma referencia cujo registro bibliografico nao corresponde a nota canonica usada para fundamentar o claim de trabalho futuro.
- Sem atualizar o indice, `lysgaard1999cluster` fica invisivel para navegacao do vault e para futuras auditorias de cobertura/BibTeX.
- Se `validation_status` continuar como `requer-validacao` em notas que a auditoria declara confirmadas por PDF, futuros gates podem reabrir o mesmo trabalho ou considerar P46 incompleto.

## Conclusao

O P46 executou a maior parte da incorporacao de conteudo com cautela academica: as notas vazias foram preenchidas, conflitos principais de DOI/ano/titulo foram tratados, e os resultados/limitacoes dos summaries foram incorporados sem transformar numeros nao revalidados em claims fortes. Mesmo assim, eu nao marcaria o roadmap como concluido ainda. Os problemas de `deepaco2023` no BibTeX, ausencia de `lysgaard1999cluster` no indice, pendencias BibTeX inconsistentes e `validation_status` contraditorio sao acionaveis antes do fechamento. Apos esses ajustes curtos, o pacote deve estar apto a PASS.
